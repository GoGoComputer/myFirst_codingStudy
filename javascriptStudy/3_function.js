// 3_function.js

'use strict';

// 함수

//f(x) = 2x + 3

    // x =1 INPUT x 들ㅇ어오는 값
            
                //------------------
                //    FUNCTION f : |
                //------------------

                            // OUTPUT (x)

// (수학) == (프로그래밍)



// f(x) = 2x + 3

    // 정의 
    function myFunction (x) {
        let temp = 2 * x + 3;
        return temp; // outuput 값

    }

    // 실행
    myFunction(1); // 5

    // 변수 이름은 맥락있게 지어주는 것이 좋다.



// f (x, y) = x + y

// 정의
// function add (x, y) {
//     let temp = x + y;
//     return temp;

// 실행
// add(1, 2); 



// 입력, 출력 없을 수 있음 (코드를 묶어주기 위해 사용)

  
                // x
                //------------------
                //    FUNCTION f : |
                //------------------
                //              x 


// 윈가디움레비오사 (  )
// 어떤 마업
// 함수
// 코디의 묶음


// 함수에요!!!

// 입력값 없음
function fly() {
    openWings();
    while (true) 
        moveWings ();
}

// (return 이 없음)

// fly()

// 정의 
function add (x, y) {
    let temp = x + y;
    return temp;
}

// 익명함수
// {(x, y)} =>  return x + y; (1, 2)

// add (x, y) = {
//     let temp = x + y;
//     return temp;

